{% extends 'base.html.twig' %}

{% block title %}Ajouter un groupe{% endblock %}

{% block body %}
{% include 'navbar.html.twig' %}
    <header>
        <h1>Ajouter un groupe</h1>
        <div class="dashboard">
            <a href="{{ path('app_band_index') }}" title="Retour"><i class="bi bi-backspace-fill"></i></a>
            {% if app.user %}
                <a href="{{ path('app_country_new') }}" class="dashboard" title="Ajouter un pays"><i class="bi bi-globe-europe-africa"></i></a>
                <a href="{{ path('app_metal_style_new') }}" class="dashboard" title="Ajouter un style de métal"><i class="bi bi-music-note-beamed""></i></i></a>
            {% endif %}
        </div>
    </header>

    <main>
        {{ include('band/_form.html.twig') }}
    </main>
    

{% endblock %}
