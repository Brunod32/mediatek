{% extends 'base.html.twig' %}

{% block title %}Modification style de métal{% endblock %}

{% block body %}
{% include 'navbar.html.twig' %}
    <header>
        <h1>Modifier {{ metal_style.name }}</h1>
        <div class="dashboard">
            <a href="{{ path('app_metal_style_index') }}" title="Retour"><i class="bi bi-backspace-fill"></i></a>
            {{ include('metal_stylee/_delete_form.html.twig') }}

        </div>
    </header>

    <main>
        {{ include('metal_style/_form.html.twig', {'button_label': 'Modifier'}) }}
    </main>

{% endblock %}
