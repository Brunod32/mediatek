{% extends 'base.html.twig' %}

{% block title %}Styles de métal{% endblock %}

{% block body %}
{% include 'navbar.html.twig' %}
    <header>
        <h1>Styles de métal</h1>
        {% if app.user %}
            <div class="dashboard">
                <a href="{{ path('app_metal_style_index') }}"><i class="bi bi-backspace-fill"></i></a>

                <a href="{{ path('app_lmetalstyle_edit', {'id': metal_style.id}) }}"><i class="bi bi-pencil-square"></i></a>

                {{ include('metal_style/_delete_form.html.twig') }}
            </div>
        {% endif %}
    </header>

    <main id="entityInfos">
        <div>
            <h3 class="mt-5">Styles de métal</h3>
            {{ metal_style.name }}
        </div>
    </main>

{% endblock %}
