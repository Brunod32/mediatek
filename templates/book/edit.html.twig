{% extends 'base.html.twig' %}

{% block title %}Modification livre{% endblock %}

{% block body %}
{% include 'navbar.html.twig' %}
    <header>
        <h1>Modifier le livre {{ book.title }}</h1>
        <div class="dashboard">
            <a href="{{ path('app_book_index') }}"><i class="bi bi-backspace-fill"></i></a>

            {{ include('book/_delete_form.html.twig') }}
        </div>
    </header>
    
    <main>
        {{ include('book/_form.html.twig', {'button_label': 'Modifier'}) }}
    </main>
    

    
{% endblock %}
