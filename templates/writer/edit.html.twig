{% extends 'base.html.twig' %}

{% block title %}Modificatio auteur{% endblock %}

{% block body %}
{% include 'navbar.html.twig' %}
    <header>
        <h1>Modifier {{ writer.firstname ~ ' ' ~ writer.lastname }}</h1>
        <div class="dashboard">
            <a href="{{ path('app_writer_index') }}"><i class="bi bi-backspace-fill"></i></a>

            {{ include('writer/_delete_form.html.twig') }}
        </div>
    </header>

    <main>
    {{ include('writer/_form.html.twig', {'button_label': 'Modifier'}) }}
    </main>
    

    
{% endblock %}
