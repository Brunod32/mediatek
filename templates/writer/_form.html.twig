{{ form_start(form) }}
    <div class="formDiv">
        <div class="formInForm">
            {{ form_row(form.firstname , { 'label': 'Prénom de l\'auteur' }) }}
            {{ form_row(form.lastname , { 'label': 'Nom de l\'auteur' }) }}
        </div>
        <div class="formInForm">
            {{ form_row(form.country , { 'label': 'Pays d\'origine' }) }}
            {{ form_row(form.picture , { 'label': 'Image de l\'auteur' }) }}
        </div>
    </div>

    <button class="btn btn-outline-primary mt-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
